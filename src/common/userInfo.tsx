export interface userInfo {
    current_to_userid: string,
    current_to_nickname: string,
    current_from_nickname: string,
    current_from_userid: string,
    group: boolean,
    group_name: string,
    group_id: string
}